export declare const PaginationMode: {
    readonly NORMAL: "PAGINATION_MODE_NORMAL";
    readonly CURSOR: "PAGINATION_MODE_CURSOR";
};
export type PaginationMode = typeof PaginationMode[keyof typeof PaginationMode];
