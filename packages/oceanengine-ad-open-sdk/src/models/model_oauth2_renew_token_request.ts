// 由 oceanengine/ad_open_sdk_go models/model_oauth2_renew_token_request.go 生成
// 不要手动编辑。

export interface Oauth2RenewTokenRequest {
  app_id: number | string;
  refresh_token: string;
  secret: string;
}

