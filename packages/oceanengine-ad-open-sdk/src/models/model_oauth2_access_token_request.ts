// 由 oceanengine/ad_open_sdk_go models/model_oauth2_access_token_request.go 生成
// 不要手动编辑。

export interface Oauth2AccessTokenRequest {
  app_id?: number | string;
  auth_code: string;
  secret: string;
}

