---
title: X-Open-Signature签名校验
platform: oceanengine
source_url: https://open.oceanengine.com/labels/7/docs/1722481732805646?origin=left_nav
doc_id: 1722481732805646
source_id: oceanengine_1722481732805646
---
# X-Open-Signature签名校验

开发者可通过对header中的“X-Open-Signature”字段进行签名校验，验证用户身份，防止信息被恶意篡改

系统会在回调请求的header中带入X-Open-Signature，该字段值是由创建订阅任务时配置的secret_key 计算得出，可通过该字段验证回调请求用户身份，防止信息被恶意篡改

# 签名示例

调用 AuthTokenUtil.isValidToken进行鉴权验证签名，失败则抛出异常或catch异常业务进行处理

## Java

SpicallbackApplication.java

```
xxxxxxxxxx
package com.bytedance.spicallback;
​
import jakarta.servlet.http.HttpServletRequest;
​
import com.bytedance.spicallback.util.AuthTokenUtil;
import com.bytedance.spicallback.vo.BaseResponse;
import com.bytedance.spicallback.vo.CallbackResponse;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.web.bind.annotation.*;
​
import java.io.IOException;
​
@SpringBootApplication
@RestController
public class SpicallbackApplication {
​
@RequestMapping(value = "/callback", method = { RequestMethod.GET, RequestMethod.POST })
public CallbackResponse callback(HttpServletRequest request,
@RequestParam(value = "challenge", defaultValue = "0") int challenge,
@RequestParam(value = "event", defaultValue = "") String event) throws IOException {
// 处理 verify 事件
if (null != event && event.equals("verify_webhook")) {
return new CallbackResponse(BaseResponse.ok(), challenge);
}
​
// 订阅任务配置页面的 "验证密钥"
String secretKey =  System.getenv("SECRET_KEY");
​
// 数据接收，验证消息
AuthTokenUtil.InputStreamCacher cacher = new AuthTokenUtil.InputStreamCacher(request.getInputStream());
boolean isValidToken =  AuthTokenUtil.isValidToken(secretKey, cacher, request.getHeader("X-Open-Signature"));
if (!isValidToken) {
return new CallbackResponse(new BaseResponse(400, "invalid token"), 0);
}
// 数据处理流程...
return new CallbackResponse(BaseResponse.ok(), 0);
}
​
public static void main(String[] args) {
SpringApplication.run(SpicallbackApplication.class, args);
}
​
​
​
}
​
```

util/AuthTokenUtil.java

```
xxxxxxxxxx
package com.bytedance.spicallback.util;
​
import com.google.common.hash.HashCode;
import com.google.common.hash.HashFunction;
import com.google.common.hash.Hashing;
import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.io.InputStream;
import  org.apache.commons.lang3.StringUtils;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
​
​
public class AuthTokenUtil {
static Logger logger = LoggerFactory.getLogger(AuthTokenUtil.class);
​
public static byte[] readAsBytes(InputStreamCacher cacher){
int len = cacher.getLength();
byte[] buffer = new byte[len];
try(InputStream in = cacher.getInputStream()){
in.read(buffer, 0, len);
}catch (IOException e){
e.printStackTrace();
}
return buffer;
}
// 根据SecretKey计算签名
public static String genSignature(String secretKey, byte[] binaryparams){
HashFunction function = Hashing.hmacSha256(secretKey.getBytes());
HashCode code = function.hashBytes(binaryparams);
return code.toString();
}
// 校验签名
public static boolean isValidToken(String secretKey, InputStreamCacher cacher, String signature ) {
if (StringUtils.isEmpty(secretKey) || StringUtils.isEmpty(signature)) {
return false;
}
String reSignature = genSignature(secretKey,readAsBytes(cacher));
logger.info("secretKey: " + secretKey);
logger.info("content: " + new String(readAsBytes(cacher)));
logger.info("signature: " + signature);
logger.info("reSignature: " + reSignature);
return signature.equals(reSignature);
}
​
public static class InputStreamCacher {
private int length;
private ByteArrayOutputStream byteArrayOutputStream = null;
​
public InputStreamCacher(InputStream inputStream) {
if (inputStream == null)
return;
length = 0;
byteArrayOutputStream = new ByteArrayOutputStream();
byte[] buffer = new byte[1024];
int len;
try {
while ((len = inputStream.read(buffer)) > -1 ) {
byteArrayOutputStream.write(buffer, 0, len);
length += len;
}
byteArrayOutputStream.flush();
} catch (IOException e) {
}
}
public InputStream getInputStream() {
if (byteArrayOutputStream== null)
return null;
return new ByteArrayInputStream(byteArrayOutputStream.toByteArray());
}
public int getLength() {
return length;
}
}
}
​
```

vo/BaseResponse.java

```
xxxxxxxxxx
package com.bytedance.spicallback.vo;
​
​
import com.fasterxml.jackson.annotation.JsonAlias;
import lombok.AllArgsConstructor;
import lombok.Data;
​
import java.io.Serializable;
​
@Data
@AllArgsConstructor
public class BaseResponse implements Serializable {
@JsonAlias("StatusCode")
private Integer statusCode;
@JsonAlias("StatusMessage")
private String StatusMessage;
​
public static BaseResponse ok() {
return new BaseResponse(0, "ok");
}
}
​
```

vo/CallbackResponse.java

```
xxxxxxxxxx
package com.bytedance.spicallback.vo;
​
import com.fasterxml.jackson.annotation.JsonAlias;
import com.fasterxml.jackson.annotation.JsonInclude;
import lombok.AllArgsConstructor;
import lombok.Data;
​
@Data
@AllArgsConstructor
public class CallbackResponse {
@JsonAlias("BaseResp")
private BaseResponse baseResp;
@JsonAlias("challenge")
@JsonInclude(JsonInclude.Include.NON_EMPTY)
private Integer challenge;
}
​
```

# 其他语言版本

## Callback处理过程

callback

```
xxxxxxxxxx
package handlers
​
import (
"context"
"fmt"
"net/http"
"os"
"strconv"
​
bizutils "bytedance.com/spicallback/biz/utils"
"github.com/cloudwego/hertz/pkg/app"
"github.com/cloudwego/hertz/pkg/common/hlog"
)
​
type CallbackResponse struct {
Challenge int64 `json:"challenge"`
BaseResp  BaseResponse
}
​
// Callback
func Callback(ctx context.Context, c *app.RequestContext) {
event := c.Query("event")
hlog.CtxInfof(ctx, "event: %v", event)
// 处理 verify 事件
if event == "verify_webhook" {
challenge := c.Query("challenge")
iChallenge, _ := strconv.ParseInt(challenge, 10, 64)
c.JSON(http.StatusOK, CallbackResponse{Challenge: iChallenge, BaseResp: NewOkResponse()})
return
}
​
// 数据接收，验证消息
body, err := c.Body()
if nil != err {
hlog.CtxWarnf(ctx, "读取 Body 失败, err: %v", err)
c.JSON(http.StatusInternalServerError, CallbackResponse{
BaseResp: NewResponse(http.StatusInternalServerError, err.Error()),
})
return
}
// 从环境变量中读取回调任务对应的 SECRET_KEY
secretKey := os.Getenv("SECRET_KEY")
util := bizutils.AuthTokenUtil{SecretKey: secretKey}
isValidToken := util.IsValidToken(ctx, body, c.GetHeader("X-Open-Signature"))
// 验签失败
if !isValidToken {
err = fmt.Errorf("invalid sinature")
hlog.CtxWarnf(ctx, "验签名失败, err: %v", err)
c.JSON(http.StatusBadRequest, CallbackResponse{
BaseResp: NewResponse(http.StatusBadRequest, err.Error()),
})
return
}
​
// 验签通过
// 数据处理流程:  写 DB、发送 MQ 等, 强烈建议数据保存后立即返回，然后再异步处理事件，避免同步处理导致回调接口超时
hlog.CtxInfof(ctx, "received valid message: %v", string(body))
​
c.JSON(http.StatusOK, CallbackResponse{
BaseResp: NewOkResponse(),
})
}
​
xxxxxxxxxx
<?php
require 'auth_token_util.php';
header('Content-Type: application/json; charset=utf-8');
​
// 处理 verify 事件
$event = $_GET['event'];
if ($event == "verify_webhook") {
$challenge = $_GET['challenge'];
$data =[
"Challenge" => (int)$challenge,
"BaseResp" => [
"StatusCode" =>  0,
"StatusMessage" =>  "ok"
]
];
echo json_encode($data);
return;
}
​
// 从环境变量中读取回调任务对应的 SECRET_KEY
$secret_key = $_ENV["SECRET_KEY"];
$util  = new AuthTokenUtil($secret_key);
​
// 数据接收，验证消息
$headers = getallheaders();
$body = file_get_contents('php://input');
$signature = $headers['x-open-signature'];
​
$is_valid_token = $util->is_valid_token($body, $signature);
// 验签失败
if (!$is_valid_token) {
$data =[
"BaseResp" => [
"StatusCode" =>  400,
"StatusMessage" =>  "invalid signature"
]
];
http_response_code(400);
echo json_encode($data);
return;
}
​
// 验签通过
// 数据处理流程:  写 DB、发送 MQ 等, 强烈建议数据保存后立即返回，然后再异步处理事件，避免同步处理导致回调接口超时
​
​
$data =[
"BaseResp" => [
"StatusCode" =>  0,
"StatusMessage" =>  "ok"
]
];
echo json_encode($data);
?>
​
```

## 签名过程

auth_token_util

```
xxxxxxxxxx
package utils
​
import (
"context"
"crypto/hmac"
"crypto/sha256"
"encoding/hex"
​
"github.com/cloudwego/hertz/pkg/common/hlog"
)
​
type AuthTokenUtil struct {
SecretKey string
}
​
func (u *AuthTokenUtil) IsValidToken(ctx context.Context, body []byte, signature []byte) bool {
if len(u.SecretKey) == 0 || len(signature) == 0 {
return false
}
reSignature := u.Signature(body)
hlog.CtxInfof(ctx, "signature: %v, reSignature: %v ", string(signature), reSignature)
return reSignature == string(signature)
}
​
func (u *AuthTokenUtil) Signature(body []byte) string {
hash := hmac.New(sha256.New, []byte(u.SecretKey))
hash.Write(body)
hashSum := hash.Sum(nil)
hashString := hex.EncodeToString(hashSum)
return hashString
}
​
xxxxxxxxxx
<?php
class AuthTokenUtil {
private $secret_key;
​
public function __construct($secret_key) {
$this->secret_key = $secret_key;
}
​
public function is_valid_token($body, $signature) {
if (strlen($this->secret_key) == 0 || strlen($signature) == 0) {
return false;
}
​
$re_signature = $this->signature($body);
return $signature == $re_signature;
}
​
public function signature($body) {
return hash_hmac('sha256', $body, $this->secret_key);
}
​
};
?>
​
```
