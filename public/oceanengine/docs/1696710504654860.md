---
title: 回调地址怎么设置和使用
platform: oceanengine
source_url: https://open.oceanengine.com/labels/7/docs/1696710504654860?origin=left_nav
doc_id: 1696710504654860
source_id: oceanengine_1696710504654860
---
# 回调地址怎么设置和使用

### 概要

#### 含义

回调地址是一个用户自定义的链接地址，用户授权完成后，Marketing-API会并将授权相关的信息和state作为参数拼接至该回调地址，并进行跳转。

#### 使用人群

#### 形式

具体形式请与技术人员沟通自定义选择。

在客户授权成功后，会生成auth_code（该字段为换取token的凭证，通过“[获取Access Token](https://open.oceanengine.com/labels/7/docs/1696710505596940)”接口可换取接口调用凭证access-token），该参数将和state一起传递给回调地址，进行程序化的统一处理。

### 回调地址作用图示

注：拼接授权地址可详见“授权”

### 回调地址使用示例

#### 后端GET接口

##### 接口定义

* URL： * [https://your.server.domain/oauth2/callback](https://your.server.domain/oauth2/callback)/ * 入参

| 字段 | 类型 | 是否必填 | 含义 |
| --- | --- | --- | --- |
| auth_code | string | 是 | 授权码 用于换取Access-Token，可作为“[获取Access Token](https://open.oceanengine.com/labels/7/docs/1696710505596940)”接口的请求参数进行调用获取访问凭证 |
| state | string | 是 | 拼接授权链接时自定义拼装的参数，回调时原样返回 一些常见用法： 1. 校验是否是指定的授权（合理性） 2. 特殊逻辑处理（指定的客户需要根据一些参数进行特殊的处理，state=need_transfer） 3. 存储指定信息（例如希望给客户存储公司信息，在引导用户授权拼接授权url时设置state=company_name） |

##### 示例代码

```
xxxxxxxxxx
# Python请求示例
APP_SECRET = "xxx"
APP_ID = 0
​
@oauth2.route("/callback/")
def callback():
auth_code = request.args.get("auth_code")
state = request.args.get("state")  # state可根据实际情况使用
​
if auth_code:
try:
get_access_token_by_auth_code(auth_code) # 使用auth_code进行token获取和存储
except Exception as err:
logging.warning("get access_token failed, err=%s", err)
​
return
​
def get_access_token_by_auth_code(auth_code):
"""
auth_code换取access-token详见https://ad.oceanengine.com/openapi/doc/index.html?id=1696710505596940
:param auth_code:
:return:
"""
import requests
open_api_url_prefix = "https://ad.oceanengine.com/open_api/"
uri = "oauth2/access_token/"
url = open_api_url_prefix + uri
data = {
"app_id": APP_ID,
"secret": APP_SECRET,
"grant_type": "auth_code",
"auth_code": auth_code
}
rsp = requests.post(url, json=data)
rsp_data = rsp.json()
save_oauth_to_db(rsp_data)    # 将access_token等信息进行存储
return
```

#### 前端界面链接

##### 示例
