---
title: 批量删除视频素材
platform: oceanengine
source_url: https://open.oceanengine.com/labels/7/docs/1696710605373455?origin=left_nav
doc_id: 1696710605373455
source_id: oceanengine_1696710605373455
---
# 批量删除视频素材

通过此接口，用户可以对素材视频进行批量删除。

当素材删除失败时，会展示在video_id列表，不在此列表内的素材表示删除成功！

### 请求地址

[https://ad.oceanengine.com/open_api/2/file/video/delete/](https://ad.oceanengine.com/open_api/2/file/video/delete/)[可视化调试](https://open.oceanengine.com/tools/visual_debug.html?docId=1696710605373455)[SDK下载](https://open.oceanengine.com/labels/7/docs/1773083358100557)[MCP调用](https://open.oceanengine.com/labels/7/docs/1847394521222599)

### 请求方法

POST

### 请求Header

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| Content-Type 必填 | string | 请求消息类型，允许值：application/json |
| Access-Token 必填 | string | 授权access-token，获取方法见接口文档[【获取Access-Token】](https://open.oceanengine.com/labels/7/docs/1696710505596940) |

### 请求参数

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| advertiser_id 必填 | number | 素材归属的客户 |
| video_ids | string[] | 待删除的video_id列表，长度范围：1 ~ 100 |
| material_ids | number[] | 待删除的material_id列表，长度范围：1 ~ 100 注意：video_ids和material_ids至少填一个 |

### 请求示例

```
xxxxxxxxxx
def delete_video():
import requests
open_api_url_prefix = "https://ad.oceanengine.com/open_api/"
uri = "2/file/video/delete/"
url = open_api_url_prefix + uri
params={
"advertiser_id": 0,
"video_ids": ["aaaa"],
}
headers = {"Access-Token": "xxx"}
rsp = requests.post(url, json=params, headers=headers)
rsp_data = rsp.json()
return rsp_data
​
xxxxxxxxxx
public JSONObject deleteVideo() {
String access_token = "xxx";
final Long advertiser_id = 0L;  //操作的广告主id
final String[] video_ids = new String[]{"11", "22", "33"};
​
// 请求地址
String open_api_url_prefix = "https://ad.oceanengine.com/open_api/2/";
String uri = "file/video/delete/";
​
// 请求参数
Map<string,object> data = new HashMap(){
{
put("advertiser_id", advertiser_id);
put("video_ids", video_ids);
}
};
​
// 构造请求
HttpEntityEnclosingRequestBase httpEntity = new HttpEntityEnclosingRequestBase() {
@Override
public String getMethod() {
return "POST";
}
};
​
httpEntity.setHeader("Access-Token",access_token);
​
CloseableHttpResponse response = null;
CloseableHttpClient client = null;
​
try {
client = HttpClientBuilder.create().build();
httpEntity.setURI(URI.create(open_api_url_prefix + uri));
httpEntity.setEntity(new StringEntity(JSONObject.toJSONString(data), ContentType.APPLICATION_JSON));
​
response = client.execute(httpEntity);
if (response != null &amp;amp;&amp;amp; response.getStatusLine().getStatusCode() == 200) {
BufferedReader bufferedReader  = new BufferedReader(new InputStreamReader(response.getEntity().getContent()));
StringBuffer result = new StringBuffer();
String line = "";
while((line = bufferedReader.readLine()) != null) {
result.append(line);
}
bufferedReader.close();
return JSONObject.parseObject(result.toString());
}
​
} catch (ClientProtocolException e) {
e.printStackTrace();
} catch (IOException e) {
e.printStackTrace();
} finally {
try {
if (response != null) {
response.close();
}
client.close();
} catch (IOException e) {
e.printStackTrace();
}
}
return null;
}
​
xxxxxxxxxx
$client = new http\Client;
$request = new http\Client\Request;
​
$body = new http\Message\Body;
$body-&amp;gt;append('{
"advertiser_id": 0,
"video_ids": ["aaaa"],
}');
​
$request-&amp;gt;setRequestUrl('https://ad.oceanengine.com/open_api/2/file/video/delete/');
$request-&amp;gt;setRequestMethod('POST');
$request-&amp;gt;setBody($body);
​
$request-&amp;gt;setHeaders(array(
'Access-Token' =&amp;gt; '{{access_token}}',
'Content-Type' =&amp;gt; 'application/json'
));
​
$client-&amp;gt;enqueue($request)-&amp;gt;send();
$response = $client-&amp;gt;getResponse();
​
echo $response-&amp;gt;getBody();
​
xxxxxxxxxx
curl -POST  https://ad.oceanengine.com/open_api/2/file/video/delete/\\
-H "Access-Token: xxx" \\
-d "advertiser_id=×××" \\
-d "video_ids=["×××","×××"]"
​
```

### 应答字段

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| code | number | 返回码,详见[【附录-返回码】](https://open.oceanengine.com/labels/7/docs/1696710760866831) |
| message | string | 返回信息,详见[【附录-返回码】](https://open.oceanengine.com/labels/7/docs/1696710760866831) |
| data | json | json返回值 |
| fail_video_ids | string[] | 操作失败的video_id列表，不在此列表内的素材表示删除成功 |
| fail_material_ids | number[] | 操作失败的fail_material_id列表，不在此列表内的素材表示删除成功 |
| request_id | string | 请求日志id |

##### 应答示例

```
xxxxxxxxxx
HTTPS/1.1 200 OK
{
"message": "OK",
"code": 0,
"data": {
"fail_video_ids": []
},
"request_id": ""
}
```
