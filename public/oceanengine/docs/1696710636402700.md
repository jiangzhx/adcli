---
title: 删除表单
platform: oceanengine
source_url: https://open.oceanengine.com/labels/7/docs/1696710636402700?origin=left_nav
doc_id: 1696710636402700
source_id: oceanengine_1696710636402700
---
# 删除表单

本接口可进行表单的删除，删除后获取表单列表时默认不获取，但是仍能通过表单实例id获取表单详情，is_del状态变为1。 注：对于当日产生了线索数据的表单不支持删除，会报错：message：has clues today

### 请求地址

[https://ad.oceanengine.com/open_api/2/clue/form/delete/](https://ad.oceanengine.com/open_api/2/clue/form/delete/)[可视化调试](https://open.oceanengine.com/tools/visual_debug.html?docId=1696710636402700)[SDK下载](https://open.oceanengine.com/labels/7/docs/1773083358100557)[MCP调用](https://open.oceanengine.com/labels/7/docs/1847394521222599)

### 请求方法

POST

### 请求Header

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| Content-Type 必填 | string | 请求消息类型，允许值：application/json |
| Access-Token 必填 | string | 授权access-token，获取方法见接口文档[【获取Access-Token】](https://open.oceanengine.com/labels/7/docs/1696710505596940) |

### 请求参数

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| advertiser_id 必填 | number | 客户id |
| instance_id 必填 | number | 表单实例ID |

### 请求示例

```
xxxxxxxxxx
def delete_clue_form():
import requests
open_api_url_prefix = "https://ad.oceanengine.com/open_api/"
uri = "2/clue/form/delete/"
real_url = open_api_url_prefix + uri
data = {
"advertiser_id": 111,
"instance_id": 111
}
rsp = requests.post(real_url, json=data)
rsp_data = rsp.json()
return rsp_data
xxxxxxxxxx
public JSONObject clueFormDelete() {
String access_token = "xxx";
final Long advertiser_id = 0L;
final long instance_id = 1L;
​
// 请求地址
String open_api_url_prefix = "https://ad.oceanengine.com/open_api/2/";
String uri = "clue/form/delete/";
​
// 请求参数
Map<string,object> data = new HashMap(){
{
put("advertiser_id", advertiser_id);
put("instance_id", instance_id);
}
};
​
// 构造请求
HttpPost httpEntity = new HttpPost(open_api_url_prefix + uri);
​
httpEntity.setHeader("Access-Token",access_token);
​
CloseableHttpResponse response = null;
CloseableHttpClient client = null;
​
try {
client = HttpClientBuilder.create().build();
httpEntity.setEntity(new StringEntity(JSONObject.toJSONString(data), ContentType.APPLICATION_JSON));
​
response = client.execute(httpEntity);
if (response != null &amp;amp;&amp;amp; response.getStatusLine().getStatusCode() == 200) {
BufferedReader bufferedReader  = new BufferedReader(new InputStreamReader(response.getEntity().getContent()));
StringBuffer result = new StringBuffer();
String line = "";
while((line = bufferedReader.readLine()) != null) {
result.append(line);
}
bufferedReader.close();
return JSONObject.parseObject(result.toString());
}
​
} catch (ClientProtocolException e) {
e.printStackTrace();
} catch (IOException e) {
e.printStackTrace();
} finally {
try {
if (response != null) {
response.close();
}
client.close();
} catch (IOException e) {
e.printStackTrace();
}
}
return null;
}
xxxxxxxxxx
$request = new HttpRequest();
$request->setUrl('https://ad.oceanengine.com/open_api/2/clue/form/delete/');
$request->setMethod(HTTP_METH_POST);
​
$request->setHeaders(array(
'Content-Type' =>'application/json',
'access_token' =>'xxx'
));
​
$request->setBody('{
"advertiser_id": 111,
"instance_id": 111
}');
​
try {
$response = $request->send();
​
echo $response->getBody();
} catch (HttpException $ex) {
echo $ex;
}
xxxxxxxxxx
curl -X POST \
https://ad.oceanengine.com/open_api/2/clue/form/delete/ \
-H 'Content-Type: application/json' \
-H 'access_token: xxx' \
-d '{
"advertiser_id": 111,
"instance_id": 111
}'
```

### 应答字段

| 字段 | 类型 | 描述 |
| --- | --- | --- |
| code | number | 返回码,详见[【附录-返回码】](https://open.oceanengine.com/labels/7/docs/1696710760866831) |
| message | string | 返回信息,详见[【附录-返回码】](https://open.oceanengine.com/labels/7/docs/1696710760866831) |
| data | json | json返回值 |
| success | bool | 删除结果，true表示成功，false表示失败 |
| request_id | string | 请求日志id |

### 应答示例

```
xxxxxxxxxx
{
"message": "OK",
"code": 0,
"data": {
"success": true
},
"request_id": "2020042715502912700000100113522"
}
​
```
